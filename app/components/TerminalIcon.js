'use babel';

import React, { PropTypes, Component } from 'react';

export default class TerminalIcon extends Component {

  static propTypes = {
  };

  render() {
    return (
      <span className='fa fa-terminal' />
    );
  }
};
